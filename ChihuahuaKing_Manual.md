# 🐕👑 吉吉國王 (Chihuahua King) - 伺服器導入手冊

> 「汪！本王來統治你的伺服器了！還不快點跪下獻上罐罐！」

歡迎邀請 **吉吉國王** 來到你的 Discord 伺服器！
這不只是一隻會汪汪叫的機器狗，而是結合了 **AI 智慧聊天**、**RPG 皇家階級系統**、**實用伺服器管理** 以及 **趣味互動** 的全方位社群總管。

---

## 🌟 核心特色：為什麼你需要吉吉國王？

### 1. 🤖 絕頂聰明的 AI 大大 (Gemini 驅動)
吉吉國王擁有真正的 AI 大腦，能根據伺服器的上下文與你進行流暢對答：
- **聊天互動**：在頻道內 `@吉吉國王`，或是訊息內提到「國王」、「吉吉」，他就會回應你！
- **記憶上下文**：國王會自動記憶最近的聊天紀錄並了解對話脈絡，不再雞同鴨講！群主可隨時開關此功能。
- **上網查資料**：支援 Google Web Search Grounding，幫你立刻搜尋最新時事（需群主開啟）。
- **多模型切換**：支援切換不同的 Gemini AI 神經網路模型。
- **上下文開關**：群主可自由選擇是否讓 AI 記憶過去的對話內容。
- **可隨時切換開關**：群主可透過指令設定專屬的「AI 聊天白名單」或開啟全頻道的「免點名聊天狂歡派對」。

### 2. 👑 沉浸式「吉吉王國」RPG 階級系統
拋棄冷冰冰的數字等級！我們擁有一套專屬的「中世紀皇家階級」：
- **發言即升級**：每次有效發言可獲得 **15 ~ 25 點經驗值**，冷卻時間為 60 秒。
- **💳 課長加成 (Server Booster)**：如果你是伺服器的加成者，獲得的經驗值會直接 **1.5 倍**！
- **🛡️ 嚴格防洗版機制**：
  - 發言字數必須大於 3 個字。
  - 不能只有表情符號或貼圖，長度計算會排除掉那些符號，必須包含實際文字。
  - 嚴禁跟自己上一句發送的內容完全相同。
- **皇家頭銜與冊封大典**：突破特定等級會晉爵，並觸發華麗的「冊封廣播」讓全服見證榮耀（群主可自由開關此公告）。
- **相關指令**：`/rank` (查看自己身家)、`/leaderboard` (查看王國排行榜)
- **公告設定**：`/setup-leveling` (管理升遷公告是否發送)

### 3. 🛠️ 巨細靡遺的伺服器日誌系統 (Logging)
不再錯過伺服器內的任何風吹草動！設定日誌頻道後，國王會擔任最盡責的監視器：
- **訊息異動**：記錄誰刪除了什麼文字、誰偷改了什麼訊息（附上修改前後對照表與跳轉連結）。
- **成員動態**：記錄加入、離開（含曾擁有的身分組）、改名、獲得或失去身分組、禁言與封鎖。
- **語音動態**：記錄誰加入了哪個語音頻道、離開語音、或在各頻道之間跳轉。
- **伺服器變動**：頻道與身分組的增減改名、專屬表情包增減、伺服器圖示 (Icon) 與名稱更動。

### 4. 😂 豐富的日常趣味互動
除了正經功能，國王也需要被安撫：
- **觸發關鍵字**：隨時發送 `摸摸國王` 或 `抱抱國王`，國王會隨機給出傲嬌反差萌回應！
- **占卜與每日運勢**：輸入 `占卜` 或 `運勢`，讓國王為你抽出今天的吉凶與幸運指數。
- **每日金句**：輸入 `每日一汪`，國王會施捨一句充滿智慧（或幹話）的名言。
- **相關指令**：`/daily` (每日領錢)、`/feed` (餵食國王)、`/poll` (發起投票)

### 5. 🎮 御用百視達 (Steam 遊戲情報)
身為遊戲玩家的最佳夥伴，國王可以幫你隨時掌握 Steam 的最新資訊：
- **查價格與評價**：使用 `/steam search` 指令並輸入遊戲名稱，國王會立刻幫你查出該遊戲在台灣 Steam 商店的價格、是否有特價、歷史折扣以及玩家評分。
- **熱門特快報**：使用 `/steam sales` 指令，國王會幫你列出目前 Steam 上最受到矚目、折扣力道最大的前 10 款熱門特價遊戲推薦！

---

## 🚀 群主快速起步指南 (Quick Start)

剛把國王邀請進伺服器？請跟著以下步驟完成基礎設定：

1. **確認權限**
   - 請確保「吉吉國王」身分組在伺服器設定中**排序夠高**，這樣他才能幫其他成員發放身分組。
   - 確保機器人擁有「讀取訊息」、「發送訊息」、「嵌入連結」等基本權限。

2. **啟用 AI 御用系統 (選用)**
   - 使用 `/ai-login` 輸入密碼解鎖 AI 管理權限。
   - 使用 `/ai-setup whitelist add` 將特定子民加入白名單，他們只要 `@` 國王就能直接聊天。
   - 可以在 `/ai-setup` 系列指令中自由設定個性 (`role`)、網路搜尋 (`search`)、所屬模型 (`model`) 等功能。

3. **設定大廳與全視之眼日誌 (選用)**
   - 使用 `/setup-log` 啟用全方位的伺服器日誌追蹤！建議設定在只有管理員能看見的隱密頻道，讓國王幫你監視一切。
   - 使用 `/setup-welcome` 設定新人報到的歡迎頻道。

4. **建立自助身分組**
   - 使用 `/reactionrole setup` 綁定按鈕身分組。
   - 使用 `/selfrole setup` 建立下拉式選單的身分組領取區。

---

## 📜 常用指令一覽 (Commands)

直接在對話框輸入 `/` 即可查看國王的所有指令。以下列出最常用的幾個：

| 指令 | 權限要求 | 說明 |
| :--- | :--- | :--- |
| `/help` | 所有人 | 📖 開啟國王的指令大典（一般子民會自動隱藏後台管理分類） |
| `/steam *` | 所有人 | 🎮 御用百視達（找特價、看情報的好幫手） |
| `/rank` | 所有人 | 🏅 查看目前的等級、經驗值與皇家頭銜 |
| `/leaderboard`| 所有人 | 🏆 查看伺服器內前十名的風雲排行榜 |
| `/poll` | 所有人 | 📊 發起一個永遠不會失效的精緻投票 |
| `/daily` | 所有人 | 💵 領取每日薪水 |
| `/ai-login` | 伺服器管理員 | 🔐 獨立的密碼驗證系統，藉此獲取 AI 控制權 |
| `/ai-setup *` | 伺服器管理員 | 🤖 AI 強大控制面板（設定白名單、性格、搜尋、模型設定等） |
| `/setup-log` | 伺服器管理員 | � 指定一個監視專用頻道，幫你記錄伺服器大小事 |
| `/announce` | 伺服器管理員 | 📢 透過彈出視窗輕鬆發布精美的王國聖旨公告 |
| `/selfrole` | 伺服器管理員 | 🏷️ 建立下拉式選單的自助身分組區 |
| `/reactionrole`| 伺服器管理員 | 🏷️ 建立點擊按鈕式的自助身分組區 |
| `/setup-leveling` | 伺服器管理員 | 📈 設定成員升級時是否要在頻道發送公告 |

---

> **👑 來自吉吉國王的聖旨：**
> 「手冊看完了嗎？看完了就快去用 \`/help\` 呼叫本王，然後去聊天室多講點話賺經驗值！汪！」
